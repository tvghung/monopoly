name: CI

on:
  push:
    branches:
      - main
      - 'overhaul/**'
      - 'codex/**'
  pull_request:
  workflow_dispatch:

# Least-privilege token: the build only needs to read the repo contents.
permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      DATABASE_URL: postgresql://monopoly:monopoly@localhost:5432/monopoly
      TEST_DATABASE_URL: postgresql://monopoly:monopoly@localhost:5432/monopoly
    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_DB: monopoly
          POSTGRES_USER: monopoly
          POSTGRES_PASSWORD: monopoly
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U monopoly -d monopoly"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    steps:
      - uses: actions/checkout@v4

      - name: Enable Corepack
        run: corepack enable

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Validate V1 release contract
        run: pnpm validate:v1-contract

      - name: Test V1 release contract
        run: pnpm test:v1-contract

      - name: Test segmented music transport
        run: pnpm test:music-transport

      - name: Install audio validation tools
        run: command -v ffmpeg && command -v ffprobe || (sudo apt-get update && sudo apt-get install -y ffmpeg)

      - name: Apply database migrations
        run: pnpm db:migrate

      - name: Typecheck
        run: pnpm typecheck

      - name: Lint
        run: pnpm lint

      - name: Test
        run: pnpm test

      - name: Build
        run: pnpm build
