# URL of the Socket.IO server. Leave empty to use the same origin as the page
# (works with the Vite dev proxy and the single-service production deploy).
# Set this only when the client and server are hosted separately.
VITE_SOCKET_URL=
