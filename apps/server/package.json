{
  "name": "@monopoly/server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "db:migrate": "node --env-file-if-exists=../../.env --import tsx src/persistence/migrate.ts",
    "db:status": "node --env-file-if-exists=../../.env --import tsx src/persistence/migrate.ts --status",
    "dev": "node --env-file-if-exists=../../.env --import tsx --watch src/index.ts",
    "start": "node --env-file-if-exists=../../.env --import tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@monopoly/shared": "workspace:*",
    "express": "^5.2.1",
    "express-rate-limit": "^8.6.2",
    "pg": "^8.16.3",
    "socket.io": "^4.8.3",
    "tsx": "^4.23.8",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^26.1.2",
    "@types/pg": "^8.15.5",
    "socket.io-client": "^4.8.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
